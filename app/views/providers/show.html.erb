Welcome <%= @provider.name  %>
<br>
Here's your current caseload:

<ul>
<% @consumers.each do |consumer|   %>
<!-- Dropdown Trigger -->
  <a class='dropdown-button btn blue' href='#' data-activates='dropdown<%=consumer.id%>'><%= consumer.name  %></a>
  <!-- Dropdown Structure -->
  <ul id='dropdown<%=consumer.id%>' class='dropdown-content'>
    <li><%= link_to 'Show', consumer_path(consumer) %></li>
    <li class="divider"></li>
    <li><%= link_to 'Edit', edit_consumer_path(consumer) %></li>
    <li class="divider"></li>
    <li><%= link_to 'Delete', consumer_path(consumer), method: :delete %></li>
  </ul>
  <br>
<% end  %>
</ul>




<div class="droppable row"><div class="col s6 m4"><div class="card blue-grey darken-1"><div class="card-content white-text"><span class="card-title">List</span><div class="row"><div class="col s1 m12"><div class="draggable card-panel white"><span class="grey-text">Draggable card</span><div class="card-action"><a href="#">Helpful</a></div></div></div></div></div></div></div></div>


<div class='draggable'></div>
<input type='button' class='content' value='Create card'/>
<div class='lists'></div>

<div class="backdrop">
  <div id="main"></div>
</div>
<script>$(Drello.initialize);</script>
<script>
$(document).ready(function(){
    $('.dropdown-button').dropdown({
    inDuration: 300,
    outDuration: 225,
    constrain_width: true, // Does not change width of dropdown to that of the activator
    hover: false, // Activate on click
    alignment: 'left', // Aligns dropdown to left or right edge (works with constrain_width)
    gutter: 0, // Spacing from edge
    belowOrigin: true // Displays dropdown below the button
    }
);


    


    $( ".draggable" ).draggable({
    	revert: "invalid", snap: "true", snapMode: "inner" 
    });
    $(".droppable").droppable({
    drop: function( event, ui ) {
           ui.helper.css('top','');
           ui.helper.css('left','');

        }
   });
    $('.content').click(function(){
        var htmlData='<div class="row"><div class="col s6 m4"><div class="droppable card blue-grey darken-1"><div class="card-content white-text"><span class="card-title">draggable list</span><div class="row"><div class="col s1 m12"><div class="draggable card-panel white"><span class="grey-text">Card Title</span><div class="card-action"><a href="#">Helpful</a></div></div></div></div></div></div></div></div>';
        $('.lists').append(htmlData);
        $( ".draggable" ).draggable();
        });
    
});
</script>
