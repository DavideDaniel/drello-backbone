<nav>
    <div class="nav-wrapper blue">
      <a href="#" class="brand-logo left">dr <i class=" z-depth-1 mdi-maps-local-hospital"></i> ello</a>
      <% if @provider  %>
      welcome <%= @provider.name  %>
    <% else %>
      <ul id="nav-mobile" class="right">
      <li><%= link_to 'Sign Up', signup_path, method: :get %> </li>
            <li><%= link_to 'Consumer Sign In', login_path, method: :get %></li>
            <li><%= link_to 'Provider Sign In', login_path, method: :get %></li>
      <% end %>

      </ul>
    </div>
  </nav>